{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "Aplicación TO-DO moderna con React y Supabase",
  "scripts": {
    "dev": "cd client && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd client && npm run preview",
    "install-all": "npm install && cd client && npm install"
  },
  "keywords": [
    "todo",
    "react",
    "supabase",
    "typescript"
  ],
  "author": "Tu nombre",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
